{  
  "name": "resume",
  "preset": "jest-puppeteer",
  "jest": {
    "verbose": true,
    "preset": "jest-puppeteer",
    "globals": {
      "PATH": "http://localhost:8080"
    }
  },
  "scripts": {
    "serve": "http-server",
    "test": "jest"
  },
  "devDependencies": {
    "jest":"^29.7.0",
    "jest-environment-jsdom":"^29.7.0",
    "jest-puppeteer": "^9.0.1",
    "puppeteer": "^21.3.8"
  },
  "collectCoverageFrom": [
    "**/*.{js,jsx}",
    "!**/node_modules/**",
    "!**/vendor/**"
  ]
}